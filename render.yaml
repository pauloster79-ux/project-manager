services:
  - type: web
    name: ai-pm-hub-web
    env: node
    plan: free
    buildCommand: "./build.sh"
    startCommand: "./start.sh"
    envVars:
      - key: NODE_ENV
        value: production
